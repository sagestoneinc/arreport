# Telegram Bot API credentials
# Get these from @BotFather on Telegram
TELEGRAM_BOT_TOKEN=your_bot_token_here
TELEGRAM_CHAT_ID=your_chat_id_here

# Task Collector Bot Configuration
# Generate a random secret for webhook verification
TELEGRAM_WEBHOOK_SECRET=your_random_secret_here
# Your app's public URL (e.g., https://arreport.example.com)
APP_BASE_URL=https://arreport.example.com
# Bot username (without @, e.g., "tgbot") for mention matching
BOT_USERNAME=tgbot
# Task storage type: "sqlite" | "mysql" | "memory" (default: sqlite)
TASKS_STORAGE=sqlite

# Authentication Configuration
# Set to 'false' to disable authentication (default: enabled)
AUTH_ENABLED=true
# Configure users in format: email:password:name:role (comma-separated for multiple users)
# Example: AUTH_USERS=admin@example.com:secretpass:Admin User:admin,user@example.com:userpass:Regular User:user
# If not set, a default admin user is created: admin@arreport.com / admin123
AUTH_USERS=admin@arreport.com:changeme:Admin:admin

# Debug API Configuration (optional)
# Set this to enable the /api/debug-storage endpoint in production
# Generate a strong random key (e.g., openssl rand -hex 32)
# DEBUG_API_KEY=your_secure_random_key_here

# MySQL Configuration (only needed if TASKS_STORAGE=mysql)
# Standard naming convention
MYSQL_HOST=localhost
MYSQL_PORT=3306
MYSQL_USER=root
MYSQL_PASSWORD=your_mysql_password
MYSQL_DATABASE=arreport

# Railway MySQL Configuration (automatically set by Railway when MySQL plugin is added)
# The application supports both MYSQL_* and MYSQL* naming conventions
# Railway automatically populates these variables using Railway's internal template variables:
# MYSQLHOST - Set to ${{RAILWAY_PRIVATE_DOMAIN}} for private network access
# MYSQLPORT - Set to 3306
# MYSQLUSER - Set to root
# MYSQLPASSWORD - Set to ${{MYSQL_ROOT_PASSWORD}} (Railway's internal auto-generated password variable)
# MYSQLDATABASE - Set to railway (default database name)
#
# Railway also constructs these connection URLs using template variables:
# MYSQL_URL - Full private connection URL: mysql://${{MYSQLUSER}}:${{MYSQL_ROOT_PASSWORD}}@${{RAILWAY_PRIVATE_DOMAIN}}:3306/${{MYSQLDATABASE}}
# MYSQL_PUBLIC_URL - Full public connection URL: mysql://${{MYSQLUSER}}:${{MYSQL_ROOT_PASSWORD}}@${{RAILWAY_TCP_PROXY_DOMAIN}}:${{RAILWAY_TCP_PROXY_PORT}}/${{MYSQLDATABASE}}
#
# Note: Railway template variables like ${{MYSQL_ROOT_PASSWORD}} are Railway's internal variables
# that get resolved and stored in environment variables like MYSQLPASSWORD at runtime.
